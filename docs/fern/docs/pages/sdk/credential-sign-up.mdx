---
title: CredentialSignUp
---

# CredentialSignUp

A form component for signing up users with credentials, including email and password inputs.

## Returns

`JSX.Element` - A React component that renders the sign-up form.

## Example

```typescript
import { CredentialSignUp } from 'your-sdk-name';

function App() {
  return <CredentialSignUp />;
}

export default App;
```