---
title: userSettings
---

# userSettings

A component that allows a user to leave a specified team.

## Parameters

- `props`: `{ team: Team }` - An object containing the team that the user wishes to leave.

## Example

```typescript
import { userSettings } from '@stackframe/stack';

const team = { /* team data */ };

<userSettings team={team} />;
```