---
title: TokenStoreInit
---

# TokenStoreInit

Defines the type for initializing a token store based on the `HasTokenStore` generic parameter.

## Type Parameters

- `HasTokenStore`: `boolean` (default: `boolean`) - A flag that determines the structure of the token store.

## Possible Values

- If `HasTokenStore` is `true`, the following types are allowed:
  - `