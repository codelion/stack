---
title: TokenStoreInit
---

# TokenStoreInit

Defines the initialization options for a token store, which can vary based on whether a token store is required.

## Parameters

- `HasTokenStore`: `boolean` - Indicates whether a token store is required. Defaults to `boolean`.

## Types

- If `HasTokenStore` is `true`, the type can be one of the following:
  - `